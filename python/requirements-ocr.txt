# OCR fallback for scanned PDFs (optional — auto-skipped if not installed).
# PDF pages are rendered by the core PyMuPDF dependency; Poppler is not required.
# Install with:  pip install -r requirements-ocr.txt
pytesseract==0.3.13
# Stable CPU inference baseline recommended by PaddleOCR's installation guide.
# GPU deployments should replace this with the matching paddlepaddle-gpu wheel.
paddlepaddle==3.2.0
paddleocr==3.5.0
